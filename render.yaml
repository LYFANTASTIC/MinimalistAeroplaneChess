services:
  - type: web
    name: minimalist-aeroplane-chess
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:frontend
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: NODE_ENV
        value: production
